name: spec

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tla:
    name: Model-check HeadDaemon
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/cache@v6
        with:
          path: ~/.cache/mentat
          key: tla2tools-1.7.4
      - run: scripts/check-spec
